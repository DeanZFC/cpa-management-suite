name: Build CPA Management Suite plugin

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test-and-build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache-dependency-path: go.sum

      - name: Run tests
        run: go test ./...

      - name: Build Linux amd64 plugin
        run: |
          CGO_ENABLED=1 GOOS=linux GOARCH=amd64 \
            go build -buildmode=c-shared -trimpath -o cpa-management-suite.so .
          rm -f cpa-management-suite.h

      - name: Upload plugin artifact
        uses: actions/upload-artifact@v4
        with:
          name: cpa-management-suite-linux-amd64
          path: cpa-management-suite.so
